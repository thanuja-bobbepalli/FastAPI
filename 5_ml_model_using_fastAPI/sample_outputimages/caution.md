run the app.py using the below command 

```uvicorn app:app --reload```
note : do not stop this keep it in running stage tehn only the stream lit works 



then run the streamli using the bellow command :

``` streamlit run frontend.py```


